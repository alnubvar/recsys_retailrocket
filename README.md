# Recommendation System for E-commerce (RetailRocket)

## 🚀 Описание проекта

Цель проекта — разработать рекомендательную систему для интернет-магазина
на основе реальных логов пользовательских действий (просмотры, корзина, покупки)
из датасета [RetailRocket].

Фокус:
- прикладной ML (applied),
- понятные метрики качества (precision@k, recall@k),
- бизнес-интерпретация результатов.

Проект является третьим в серии:
1. NLP: Detection of Ads in Telegram + Intent Classification
2. Time Series: Retail Sales Forecasting + Anomaly Detection
3. **Current**: Recommendation System for E-commerce

---

## 🧩 Бизнес-контекст и постановка задачи

Онлайн-ритейлер собирает события пользователей на сайте:
- просмотр товара,
- добавление в корзину,
- покупка.

**Бизнес-цель** — увеличить конверсию и выручку за счёт персонализированных рекомендаций:
- что показать пользователю в текущей сессии,
- какие товары могут заинтересовать его на основе истории.

**ML-задача** — построить Top-N рекомендательную систему:
- на входе — логи взаимодействий (user/session, item, timestamp, тип события),
- на выходе — ранжированный список товаров для каждого пользователя/сессии.

---

## 🗂 Данные

Используется публичный датасет **RetailRocket**:

- `events.csv` — логи событий:
  - `timestamp`
  - `visitorid` (пользователь/сессия)
  - `event` (view, addtocart, transaction)
  - `itemid`
  - `transactionid` (только для покупок)

- `item_properties_part*.csv` — свойства товаров:
  - `itemid`
  - `property` (например, categoryid, available, price и др.)
  - `value`

- `category_tree.csv` — дерево категорий:
  - `categoryid`
  - `parentid`

В проекте выполняется:
- базовая очистка и фильтрация,
- агрегация логов по пользователям/товарам,
- формирование выборок для обучения и оценки.

---

## 🏗 Структура проекта

- `data/` — данные:
  - `raw/` — сырые csv-файлы,
  - `interim/` — промежуточные версии,
  - `processed/` — финальные датасеты для моделей.
- `notebooks/` — исследовательские ноутбуки (EDA, baseline, модели).
- `src/` — модульный код:
  - `data/` — загрузка и препроцессинг,
  - `models/` — реализация моделей рекомендаций,
  - `evaluation/` — метрики и процедуры offline-оценки.
- `reports/` — графики и итоговые выводы.
- `configs/` — конфигурация сплитов и параметров.

---

## 🔍 План экспериментов

1. **EDA**
   - Анализ распределения событий по типам (view/cart/purchase).
   - Распределение по пользователям и товарам (long tail).
   - Поведение активных и неактивных пользователей.
   - Sparsity user–item взаимодействий.

2. **Baseline-модели**
   - Простая популярность (global top-N).
   - Популярность по категориям.
   - Co-occurrence / item-to-item рекомендации.

3. **Collaborative Filtering**
   - User-based / item-based CF.
   - Matrix factorization (implicit feedback).

4. **Модели ранжирования (опционально)**
   - Формирование пар/троек (positive vs negative items).
   - Обучение модели ранжирования (например, LightFM / xgboost ranker).

5. **Метрики и оценка**
   - Оценка по top-N метрикам:
     - `precision@k`,
     - `recall@k`,
     - по желанию `MAP@k`, `NDCG@k`.
   - Оценка на разных сегментах пользователей и товаров.

---

## 📊 Метрики

Основные метрики:
- **precision@k** — какая доля рекомендованных товаров действительно была куплена,
- **recall@k** — какую долю покупок система смогла “угадать” в топ-k,
- дополнительные ранжирующие метрики по желанию.

---

## 💼 Бизнес-интерпретация

В итоговом отчёте рассматривается:
- прирост качества рекомендаций по сравнению с простым baseline (популярность),
- какие сегменты пользователей выигрывают больше всего,
- какие группы товаров чаще всего попадают в рекомендации,
- какие ограничения и риски у построенной системы.

---

## 🧱 Технологии

- Python, pandas, numpy
- SciPy / scikit-learn
- implicit / LightFM (по необходимости)
- Matplotlib / seaborn / plotly

---

## 👨‍💻 Автор

Проект выполнен как часть портфолио прикладных ML-проектов
(детекция рекламы, прогнозирование продаж, рекомендательные системы).
